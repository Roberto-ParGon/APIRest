<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="profesor" >

    <select id="obtener-todos" resultType="pojo.Profesor">
        select nombre, apellidoPaterno, apellidoMaterno, noPersonal, fechaNacimiento, fechaContratacion, profesor.idRol, rol 
        from profesor 
        inner join rol r 
        on r.idRol = profesor.idRol;
    </select>
    
</mapper>
